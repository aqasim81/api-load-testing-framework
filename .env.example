# =============================================================================
# LoadForge — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# These variables are read by loadforge._internal.config.load_config().
# All variables are optional — sensible defaults are used when unset.
# =============================================================================

# Default base URL for scenarios that don't specify one.
# Example: https://api.staging.example.com
LOADFORGE_BASE_URL=

# Maximum number of concurrent connections per worker.
# Must be a positive integer. Default: 100
LOADFORGE_POOL_SIZE=100

# Default HTTP request timeout in seconds.
# Must be a positive number. Default: 30.0
LOADFORGE_TIMEOUT=30.0
